# MedInventory - Dependencies
# by Studio Bergamaschi
#
# Intervalli ammessi, non versioni di esercizio: ogni riga ha un minimo e un
# massimo, cosi' una installazione futura non puo' tirarsi dentro una major
# mai provata (B03). Le versioni con cui il programma e' stato provato stanno
# in requirements.lock.txt, ed e' quello che si installa in produzione:
#
#     pip install -r requirements.lock.txt

# Web framework
flask>=3.0,<4.0

# AI integration (Claude API)
anthropic>=0.40,<1.0

# Excel read/write
openpyxl>=3.1,<4.0

# PDF generation
fpdf2>=2.8,<3.0

# PDF text extraction & manipulation
pdfplumber>=0.11,<0.12
pypdf>=4.0,<8.0

# HTTP client (test connessione provider AI in strutture_bp/admin)
httpx>=0.27,<1.0

# Encryption for IMAP credentials
cryptography>=42.0,<47.0

# Production WSGI server (Windows-compatible)
waitress>=3.0,<4.0

# Windows launcher with system tray icon
pystray>=0.19,<0.20
Pillow>=10.0,<12.0

# QR code generation
qrcode[pil]>=7.4,<9.0

# CSRF protection
flask-wtf>=1.2,<2.0
